"""College football forecasts with an explicit authority gate.

The model does not beat the closing market, and `authority.current()` says so
rather than leaving the caller to find out. See reports/BASELINE_2019_2025.md.
"""

__version__ = "0.1.0"
